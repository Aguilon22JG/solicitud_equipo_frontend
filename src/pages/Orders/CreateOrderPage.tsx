import React from 'react';
import { Typography, Box } from '@mui/material';

const CreateOrderPage: React.FC = () => {
  return (
    <Box>
      <Typography variant="h4">Nueva Orden de Equipo</Typography>
      <Typography>Formulario para crear nueva solicitud de equipo</Typography>
    </Box>
  );
};

export default CreateOrderPage;