import React from 'react';
import { Typography } from '@mui/material';

const OrderDetailsPage: React.FC = () => (
  <div><Typography variant="h4">Detalles de Orden</Typography></div>
);

export default OrderDetailsPage;